version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=InventoryTrackingSystem_Identity
      - InventoryTrackingSystem_DB=InventoryTrackingSystem_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
